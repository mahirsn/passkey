post_install() {
    echo "Now run, as your user:  passkey setup"
}

post_upgrade() {
    systemctl try-restart passkeyd.service 2>/dev/null || true
}

# Undoes `passkey setup` (PAM lines, service, registrations) while the
# command still exists.
pre_remove() {
    passkey uninstall || true
}
